"use client"

export default function Divider({data}) {

    // -----------------------------------------------------------------------------------------------------------------

    return (
        <div className={`widget-divider ${data || 'mt-3-75'}`}/>
    )
}
